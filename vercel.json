{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "turbo run build --filter=@kan/web --filter=@kan/api --filter=@kan/db --filter=@kan/shared --filter=@kan/auth --filter=@kan/stripe --filter=@kan/logger",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
